public enum TreeType { // enum is a special class in Java that represents a group of constants (unchangeable variables, like final variables).
    OAK,
    MAPLE,
    PECAN,
    WALNUT,
    PINE,// The only Treetypes that can exist are those listed in the ernum class
}
 